{
  "name": "lint-pr-title",
  "version": "0.0.1",
  "description": "Ensure your PR title matches the commit lint specification.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "yarn && rm -rf dist && ncc build index.js --license licenses.txt && cp node_modules/@commitlint/config-validator/lib/commitlint.schema.json dist/"
  },
  "keywords": [
    "lint-pr-title",
    "github-action",
    "commitlint"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@commitlint/config-validator": "^19.0.3",
    "@commitlint/lint": "^19.2.2",
    "@octokit/core": "^6.1.2",
    "@octokit/graphql": "^8.1.1"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
