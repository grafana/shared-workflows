{
  "name": "signed-commits-info",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that reports PR commits without verified signatures",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/main.ts --target=node --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "eslint ."
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/bun": "latest",
    "eslint": "10.6.0",
    "typescript": "6.0.3"
  }
}
