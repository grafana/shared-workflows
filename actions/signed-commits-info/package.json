{
  "name": "signed-commits-info-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that reports PR commits without verified signatures",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/main.ts --target=node --outfile=dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "6.0.3"
  }
}
