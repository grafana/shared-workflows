name: Push to artifact registry MultiArch
description: Resuable workflow to push to Google Artifact Registry using runners of different architectures.

on:
  workflow_call:
    inputs:
      registry:
        description: |
          Google Artifact Registry to store docker images in.
        default: "us-docker.pkg.dev"
        type: string
      tags:
        description: |
          List of Docker images to be pushed.
        required: true
        type: string
      context:
        description: |
          Path to the Docker build context.
        default: "."
        type: string
      environment:
        description: |
          Environment for pushing artifacts (can be either dev or prod).
        default: dev
        type: string
      image_name:
        description: |
          Name of the image to be pushed to GAR.
        required: true
        type: string
      build-args:
        description: |
          List of arguments necessary for the Docker image to be built.
        default: ""
        type: string
      push:
        description: |
          Whether to push the image to the registry.
        required: false
        default: ${{ github.event_name == 'push' }}
        type: boolean
      file:
        description: |
          The dockerfile to use.
        required: false
        type: string
      platforms:
        description: |
          List of platforms to build the image for
        required: false
        type: string
      cache-from:
        description: |
          Where cache should be fetched from
        required: false
        default: "type=gha"
        type: string
      cache-to:
        description: |
          Where cache should be stored to
        required: false
        default: "type=gha,mode=max"
        type: string
      ssh:
        description: |
          List of SSH agent socket or keys to expose to the build
        type: string
      build-contexts:
        description: |
          List of additional build contexts (e.g., name=path)
        required: false
        type: string
      docker-buildx-driver:
        description: |
          The driver to use for Docker Buildx
        required: false
        default: "docker-container"
        type: string
      repository_name:
        description: |
          Override the 'repo_name' used to construct the GAR repository name. Only necessary when the GAR includes a repo name that doesn't match the GitHub repo name.
        required: false
        type: string
      labels:
        description: |
          List of custom labels to add to the image as metadata.
        required: false
        type: string
      target:
        description: |
          Sets the target stage to build
        required: false
        type: string
      delete_credentials_file:
        description: |
          Delete the credentials file after the action is finished.
          If you want to keep the credentials file for a later step, set this to false.
        default: "true"
        type: string
      secrets:
        description: |
          Secrets to expose to the build. Only needed when authenticating to private repositories outside the repository in which the image is being built.
        required: false
        type: string
      load:
        description: |
          Whether to load the built image into the local docker daemon.
        required: false
        default: "false"
        type: string
      server-size:
        description: "Size of the hosted runner"
        required: false
        default: "small"
        type: string
      pre-build-script:
        description: "A script to run before docker build"
        required: false
        default: ""
        type: string
    outputs:
#      version:
#        description: "Generated Docker image version (from docker/metadata-action)"
#        value: ${{ steps.meta.outputs.version }}
#      tags:
#        description: "Generated Docker tags (from docker/metadata-action)"
#        value: ${{ steps.meta.outputs.tags }}
#      labels:
#        description: "Generated Docker labels (from docker/metadata-action)"
#        value: ${{ steps.meta.outputs.labels }}
#      annotations:
#        description: "Generated annotations (from docker/metadata-action)"
#        value: ${{ steps.meta.outputs.annotations }}
#      json:
#        description: "JSON output of tags and labels (from docker/metadata-action)"
#        value: ${{ steps.meta.outputs.json }}
#      imageid:
#        description: "Image ID (from docker/build-push-action)"
#        value: ${{ steps.build.outputs.imageid }}
#      digest:
#        description: "Image digest (from docker/build-push-action)"
#        value: ${{ steps.build.outputs.digest }}
#      metadata:
#        description: "Build result metadata (from docker/build-push-action)"
#        value: ${{ steps.build.outputs.metadata }}
      platform-os-list:
        description: "The list of OS used to build images"
        value: ${{ jobs.prepare-matrix.outputs.platforms_list }}


jobs:
  prepare-matrix:
    runs-on: ubuntu-x64-small
    outputs:
      platforms_list: ${{ steps.matrix.outputs.platforms_list }}
    steps:
      - id: matrix
        run: |
          # Convert input string like "linux/amd64,linux/arm64" into JSON array
          MATRIX=$(echo "$PLATFORMS" | tr -d ' ' | tr ',' '\n' \
            | jq -R 'if .=="linux/amd64" then "x64" elif .=="linux/arm64" then "arm64" else . end' \
            | jq -s -c .)

          echo "Matrix JSON: $MATRIX"

          # Set output
          echo "platforms_list=$MATRIX" >> $GITHUB_OUTPUT
        env:
          PLATFORMS: ${{ inputs.platforms }}

  build-and-push:
    needs: prepare-matrix
    strategy:
      fail-fast: false
      matrix:
        arch: ${{ fromJson(needs.prepare-matrix.outputs.platforms_list) }}
    runs-on: ubuntu-${{ matrix.arch }}-${{ inputs.server-size }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          persist-credentials: false
      - name: Run custom build script if it exists
        if: ${{ inputs.pre-build-script != '' }}
        run: |
          set -euo pipefail
          echo "Running $PRE_BUILD_SCRIPT_PATH"

          if [ -f "$PRE_BUILD_SCRIPT_PATH" ]; then
            chmod +x "$PRE_BUILD_SCRIPT_PATH"
            "$PRE_BUILD_SCRIPT_PATH"
          else
            echo "No script found at $PRE_BUILD_SCRIPT_PATH, skipping."
          fi
        env:
          PRE_BUILD_SCRIPT_PATH: ${{ inputs.pre-build-script }}
      - name: Build Docker Image
        id: build-docker-image
        uses: grafana/shared-workflows/actions/push-to-gar-docker@rwhitaker/multi-arch-builds
        with:
          # from inputs
          registry: ${{ inputs.registry }}

          context: ${{ inputs.context }}
          environment: ${{ inputs.environment }}
          image_name: ${{ inputs.image_name }}
          build-args: ${{ inputs.build-args }}
          file: ${{ inputs.file }}
          cache-from: ${{ inputs.cache-from }}
          cache-to: ${{ inputs.cache-to }}
          ssh: ${{ inputs.ssh }}
          build-contexts: ${{ inputs.build-contexts }}
          docker-buildx-driver: ${{ inputs.docker-buildx-driver }}
          repository_name: ${{ inputs.repository_name }}
          labels: ${{ inputs.labels }}
          target: ${{ inputs.target }}
          delete_credentials_file: ${{ inputs.delete_credentials_file }}
          secrets: ${{ inputs.secrets }}

          # hardcoded
          push: false
          load: true
          platforms: ${{ matrix.arch == 'x64' && 'linux/amd64' || 'linux/arm64' }}
          tags: |-
            ${{ github.ref_name }}-${{ matrix.arch }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
#      - name: Login to GAR
#        uses: grafana/shared-workflows/actions/login-to-gar@rwhitaker/multi-arch-builds
#        with:
#          environment: dev
#          delete_credentials_file: false
#      - name: Push to GAR
#        run: |
#          echo docker push ${{ steps.build-docker-image.outputs.tags }}
#          docker push ${{ steps.build-docker-image.outputs.tags }}

#  push-manifest:
#    runs-on: ubuntu-x64-small
#    needs: build-and-push
#    permissions:
#      contents: read
#      id-token: write
#    steps:
#      - name: Login to GAR
#        uses: grafana/shared-workflows/actions/login-to-gar@rwhitaker/multi-arch-builds
#        with:
#          environment: dev
#          delete_credentials_file: false
#
#      - name: Create and push manifest
#        run: |
#          SAFE_TAG="${TAG//\//-}"
#          echo "Using sanitized tag: $SAFE_TAG"
#          IMAGE="us-docker.pkg.dev/grafanalabs-dev/docker-cicd-test-dev/cicd-test"
#          CMD="docker manifest create $IMAGE:$SAFE_TAG"
#
#          for PLATFORM in $(echo "$PLATFORMS" | tr ',' ' '); do
#            case "$PLATFORM" in
#              linux/amd64)
#                CMD="$CMD --amend $IMAGE:${SAFE_TAG}-x64"
#                ;;
#              linux/arm64)
#                CMD="$CMD --amend $IMAGE:${SAFE_TAG}-arm64"
#                ;;
#            esac
#          done
#
#          echo "Running: $CMD"
#          eval "$CMD"
#          docker manifest push $IMAGE:$SAFE_TAG
#        env:
#          TAG: ${{ github.ref_name }}
#          PLATFORMS: ${{ inputs.platforms }}
