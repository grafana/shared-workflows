name: Test missing permissions

on:
  workflow_dispatch:

jobs:
  missing-permissions-test:
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "This job tests the missing-permissions PR comment."

  write-all-test:
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Do nothing
        run: echo "This job tests that high-severity findings are still handled."
